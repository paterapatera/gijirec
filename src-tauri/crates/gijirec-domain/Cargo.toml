[package]
name = "gijirec-domain"
version.workspace = true
edition.workspace = true

[features]
contract-test-support = ["dep:serde_json"]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = { version = "1", optional = true }

[dev-dependencies]
serde_json = "1"

[lints]
workspace = true
